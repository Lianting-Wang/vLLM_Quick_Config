# Copy this file to .env. The real .env file is ignored by Git.
# Administration authentication is always enabled, including localhost access.

# May contain Unicode and spaces when quoted, for example:
# ADMIN_PASSWORD="中文 管理密码"
ADMIN_PASSWORD=

# Generate with: openssl rand -hex 32
# Must contain at least 32 characters.
SESSION_SECRET=
